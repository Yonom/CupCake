<controls:MetroWindow x:Class="CupCake.Client.Windows.ProfilesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    TitleCaps="False"
    Title="Manage Profiles"
    ResizeMode="NoResize"
    Width="400"
    SizeToContent="Height"
    NonActiveGlowBrush="#3E3E40"
    GlowBrush="{DynamicResource BackgroundSelected}"
	Background="{StaticResource LightBackground}"
    WindowStartupLocation="CenterOwner"
    SnapsToDevicePixels="True">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Button Content="Attach"  Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Height="30" Margin="0,5"/>
        <ListView Grid.ColumnSpan="2" HorizontalAlignment="Left" Height="299.667" Margin="0,0.333,0,0" VerticalAlignment="Top" Width="364">
        	<ListView.View>
        		<GridView>
        			<GridViewColumn/>
        		</GridView>
        	</ListView.View>
        </ListView>
    </Grid>
</controls:MetroWindow>


